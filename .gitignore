# 카드 이미지 원본 스캔본 (22MB) — 실제 서비스에는 assets/cards/의 리사이즈본만 쓰임.
# 재생성하려면 scripts/prepare_cards.py 실행 (raw 폴더가 없으면 자동으로 다시 받음).
assets/cards-source/raw/

# OS/에디터 캐시 파일
.DS_Store
Thumbs.db

# Netlify CLI 로컬 캐시
.netlify/

# 워터마크 있는 원본 첨부 이미지 — 더 이상 앱에서 안 쓰임, 공개 저장소에 올리지 않음
타로카드.png
# 옛 백업본 — index.html과 동일했으나 이번 업데이트는 반영 안 됨(위 이미지가 base64로 박혀있어 제외)
MysticTarot.html
